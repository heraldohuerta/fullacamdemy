
<div class="card-body text-center">
  <h1 class="card-title"><%= plan.descripcion %></h1>
  <h2 class="card-text text-danger">Precio del Plan:$<%= plan.precio.to_i %> - <%= plan.moneda %></h2>
  <hr>
  <div class = "text-left">

    <% plan.plans_options.each do |po| %>
    <div class="row">
      <div class="col-sm-2">
        <%= po.option.codigo %>
      </div>
      <div class="col-sm-6">
        <%= po.option.descripción %>
      </div>
      <div class="col-sm-2">
        <%= po.option.cantidad %>
      </div>
      <div class="col-sm-2">
        <%= po.option.unit.codigo %>
      </div>
    </div>
    <% end %>

  </end>
</div>

<% plan.course_plans.each do |cp| %>
<hr>
<div class="row mt-3">
  <div class="col-sm-3">
    <h3 class="card-text text-danger"><%= cp.course.nombre %></h3>
    <img class="card-img-top" src="<%= cp.course.firmware_image %>" alt="Card image cap">
  </div>
  <div class="col-sm-5 text-left">
    <% cp.course.asignaturs.each do |as| %>
    <p  class='m-0'>Asignatura:<%= as.nombre %></p>
    <br>
    <% as.contents.each do |co| %>
    <p class='pl-1 m-0'>Contenido:<%= co.nombre %></p>
    <p class='pl-2 m-0'>Pruebas</p>
    <ol>
      <% co.tests.each do |te| %>
      <li class='ml-4 mt-0'><%= te.nombre %></li>
      <% end %>
    </ol>
    <% end %>
    <% end %>
  </div>

</div>
<% end %>
</div>
